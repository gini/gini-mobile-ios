name: Check Health API Library
on: [push]
#  push:
#    paths:
#      - 'HealthAPILibrary/**'

jobs:
  check-dependencies:
  
    runs-on: ubuntu-latest

    steps:
    - name: swiftpackage-dependencies-check
      uses: MarcoEidinger/swift-package-dependencies-check@1.1.0
