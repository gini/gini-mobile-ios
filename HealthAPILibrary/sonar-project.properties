sonar.projectKey=gini_mobile_ios_healthAPILibrary_sdk
sonar.organization=gini

# Project metadata
sonar.projectName=iOS Health API Library
sonar.projectVersion=1.0

sonar.sources=GiniHealthAPILibrary/Sources
sonar.tests=GiniHealthAPILibrary/Tests
sonar.exclusions=**/Pods/**,**/*.xcassets/**,**/Resources/**,**/*.storyboard,**/*.xib,**/Tests/**,**/*Cell.swift
sonar.test.exclusions=**/Pods/**,**/*.xcassets/**

sonar.sourceEncoding=UTF-8

# The following file types contain only UIKit/SwiftUI rendering code that cannot
# be exercised by unit tests and must be excluded from coverage metrics:
#
#   *View.swift              — SwiftUI view bodies (rendered at runtime)
#   *ViewController.swift    — UIKit VCs (UI lifecycle, layout only)
#   *Cell.swift              — UIKit cell subclasses (layout only)
sonar.coverage.exclusions=**/Pods/**,\
  **/*View.swift,\
  **/*ViewController.swift,\
  **/*Cell.swift

# Coverage
sonar.coverageReportPaths=coverage.xml
