import XCTest
@testable import HealthAPILibrary

final class HealthAPILibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HealthAPILibrary().text, "Hello, World!")
    }
}
